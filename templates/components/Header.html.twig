<div{{attributes}}>
	<!-- component html -->
	<a href="{{ path('app_register')}}">
		register
	</a>
	<a href="{{ path('app_login')}}">
		login
	</a>
<a href="{{ path('app_createArea')}}">

		create area
	</a>
</div>

