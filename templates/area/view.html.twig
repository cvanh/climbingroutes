{% extends 'base.html.twig' %}

{% block title %}climbing area overview
{% endblock %}

{% block body %}
	<twig:Header/>
	<div class="container">
		<a href={{path('app_area')}}>go back</a>

		<ul>
			<li>name: {{area.name}}</li>
			<li>location: {{area.location}}</li>
			<li>rock type: {{area.rock_type}}</li>
		</ul>
		<p>{{area.description}}</p>
		<span>written by:
			{{area.author.email}}</span>


		{{form(mediaForm)}}


			{{dump(media)}}


			{{dump(area)}}


		</div>
	{% endblock %}
